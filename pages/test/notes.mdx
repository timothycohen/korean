import { Layout, ColorsStrip } from 'lib/components/markdown';
import { AnalogClock } from 'lib/components/time';

# Notes

Setting up a markdown layout to write info about each of the topics the app covers. Should be easy to import components as needed.

<AnalogClock hour={1} minute={3} size={300} />
<ColorsStrip />

export default ({ children }) => <Layout>{children}</Layout>;
